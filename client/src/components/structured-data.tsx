// Structured data (JSON-LD) is now rendered as static <script> tags in client/index.html
// so it is visible to crawlers that don't execute JavaScript.
export function StructuredData() {
  return null;
}
